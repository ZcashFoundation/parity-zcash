language: rust

dist: xenial

branches:
  only:
  - master

rust:
  - stable
  - nightly

matrix:
  allow_failures:
    - rust: nightly

  fast_finish: true

  include:

  - rust: nightly
    before_script:
    - rustup component add rustfmt-preview
    script:
    - cargo fmt --all -- --check

cache:
  directories:
  - "$HOME/.rustup"
  - "$HOME/.cargo"

script:
- cargo check --all
- cargo test --all
- cargo build --all
